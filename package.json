{
  "name": "turtle",
  "version": "1.0.0",
  "description": "control a mc turtle using a discord bot",
  "main": "src/index.ts",
  "scripts": {
    "build": "npx tsc",
    "server": "node build/index.js",
    "test": "npm run build && npm run server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@discordjs/rest": "0.1.0-canary.0",
    "@types/node": "^17.0.1",
    "discord-api-types": "^0.25.2",
    "discord.js": "^13.3.1",
    "typescript": "^4.5.4",
    "ws": "^8.3.0"
  },
  "devDependencies": {
    "@types/ws": "^8.2.2"
  }
}
